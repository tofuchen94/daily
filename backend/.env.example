# ==============================
# 本地开发环境变量配置示例
# ==============================
# 复制此文件为 .env，填入你的实际值
# 然后在 IDE 或命令行中加载这些变量。
#
# ⚠️ 注意：此文件中填的是示例值，使用前请替换为你自己的连接信息！
#
# 使用方式：
#   IDEA: Run/Debug Configurations → Environment variables → 粘贴
#   CMD:  set DEV_MYSQL_HOST=xxx  （每个变量单独设）
#   PowerShell:  $env:DEV_MYSQL_HOST="xxx"
#   Git Bash:    export DEV_MYSQL_HOST=xxx

# MySQL 连接信息（替换为你的实际值）
DEV_MYSQL_HOST=your-mysql-host
DEV_MYSQL_PORT=3306
DEV_MYSQL_DB=daily
DEV_MYSQL_USER=your-username
DEV_MYSQL_PASSWORD=your-password
